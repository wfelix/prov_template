/* eslint-disable no-undef */
import React from 'react';
import { render } from '@testing-library/react';

import ProductList from '../../pages/ProductList';

describe('ProductList Page', () => {
  it('should be able to product list', () => {});
});
