/* eslint-disable no-undef */
import React from 'react';
import { render } from '@testing-library/react';

import StoreLocator from '../../pages/StoreLocator';

describe('StoreLocator Page', () => {
  it('should be able to store locator', () => {});
});
