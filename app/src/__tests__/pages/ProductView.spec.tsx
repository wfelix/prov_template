/* eslint-disable no-undef */
import React from 'react';
import { render } from '@testing-library/react';

import ProductView from '../../pages/ProductView';

describe('ProductView Page', () => {
  it('should be able to product view', () => {});
});
