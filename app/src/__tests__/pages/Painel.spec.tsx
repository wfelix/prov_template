/* eslint-disable no-undef */
import React from 'react';
import { render } from '@testing-library/react';

import Painel from '../../pages/Painel';

describe('Painel Page', () => {
  it('should be able to painel', () => {});
});
