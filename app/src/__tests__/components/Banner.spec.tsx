/* eslint-disable no-undef */
import React from 'react';
import { render } from '@testing-library/react';

import Banner from '../../components/Banner';

describe('Banner Component', () => {
  it('should be able to banner', () => {});
});
